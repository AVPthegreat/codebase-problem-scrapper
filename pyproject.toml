[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tcg-desktop"
version = "0.1.0"
description = "Desktop tool for scraping coding problems and packaging testcases"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Proprietary" }
authors = [{ name = "Test Case Generator Team" }]
dependencies = [
    "PySide6>=6.7,<7.0",
    "httpx>=0.25,<0.28",
    "selectolax>=0.3.17,<0.4.0",
    "beautifulsoup4>=4.12,<4.14",
    "tenacity>=8.2,<9.0",
    "rich>=13.6,<14.0",
    "python-slugify>=8.0,<9.0",
    "aiofiles>=23.2,<24.0",
    "pydantic>=2.6,<3.0",
    "fastapi>=0.115,<0.116",
    "uvicorn[standard]>=0.30,<0.32",
    "jinja2>=3.1,<4.0"
]

# Required by FastAPI for HTML form parsing
python-multipart = ">=0.0.9,<0.0.11"

[project.optional-dependencies]
dev = [
    "pytest>=8.3,<9.0",
    "coverage>=7.6,<8.0",
    "ruff>=0.6,<0.7"
]

[project.gui-scripts]
tcg-desktop = "app.main:run"

[project.scripts]
tcg-web = "webapp.main:run"

[tool.setuptools.package-dir]
"" = "src"

[tool.setuptools]
packages = { find = { where = ["src"], include = ["app*", "webapp*"], exclude = ["tests*"] } }

[tool.pytest.ini_options]
pythonpath = ["src"]
